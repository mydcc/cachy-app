Based on the objective of scanning the codebase to assess the status quo, identify vulnerabilities, and raise the codebase to an "institutional grade" level, we have identified a set of findings for cachy-app, a high-frequency trading software.
We have found:
- Memory leak and data inconsistencies in NewsService deduplication (using title + URL vs. generating ID and limiting to 100 items for IDB caching while not limiting IDB reads)
- Zombie request handling in MarketWatcher
- Missing i18n keys and hardcoded string outputs in TradeService

Plan:
1. Fix NewsService deduplication by relying directly on the `id` generated by `generateNewsId` everywhere and using this consistently, especially replacing `item.url` checks with rigorous validations. Update sentiment caching to use a strong hash instead of just the first news title, and prevent infinite cache bloat.
2. Fix TradeService missing i18n keys for `toastService.error` and `BitunixApiError`. Use `_` from `svelte-i18n` to show localized error strings.
3. Fix potential Decimal mapping bugs in MarketWatcher gap filling or ensure correctness.
4. Clean up `MarketWatcher` zombie timeouts correctly.

I'll write tests and refactor.
