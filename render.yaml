services:
  - type: web
    name: cachy-app
    env: node
    # Explicitly use npm ci and run build. Audit false skips vulnerability check which can fail CI.
    buildCommand: npm ci --audit=false && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.18.3
